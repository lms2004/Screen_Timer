
项目结构核心    (MVC)
app.py - 主程序入口
作用：初始化并启动应用程序，加载控制器和视图。

model/ - 模型层
作用：管理业务逻辑和数据处理（如计时器功能）。

view/ - 视图层
作用：负责用户界面的显示和布局（窗口、菜单、计时器显示等）。

controller/ - 控制器层
作用：处理用户输入，将其传递给模型，并更新视图。


项目工作流程
用户操作 → 控制器处理 → 模型更新 → 视图反馈